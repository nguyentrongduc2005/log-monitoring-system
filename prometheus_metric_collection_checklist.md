# Checklist: Prometheus & Metric Collection

- [x] **1. Infrastructure & Prometheus Config**
  - [x] Add Prometheus container to `compose.yml`.
  - [x] Create `prometheus.yml` with `http_sd_configs` pointing to Backend `/internal/prometheus/targets`.
- [/] **2. Database Schema**
  - [x] Create Flyway migration script `V15__create_metric_sources.sql` for `identity.metric_sources`.
- [ ] **3. Backend Domain & Logic (Identity Module)**
  - [ ] Create `MetricSourceEntity` and `MetricSourceRepository`.
  - [ ] Implement `MetricSourceService` (CRUD logic + `testConnection`).
- [ ] **4. Backend APIs**
  - [ ] Create `MetricSourceController` for public CRUD and test connection endpoints.
  - [ ] Create internal controller for `GET /internal/prometheus/targets` (Service Discovery).
- [ ] **5. Frontend UI**
  - [ ] Add `Metric Source` types and adapter functions.
  - [ ] Build `MetricSourceConfig` UI Component (Form + UP/DOWN status).
  - [ ] Integrate tab into Application Details page.
- [ ] **6. Verification**
  - [ ] Run backend unit tests and compilation.
  - [ ] Run frontend lint and tests.
  - [ ] Git commit all changes according to constraints.
